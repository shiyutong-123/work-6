diff --git a/.github/workflows/build.yml b/.github/workflows/build.yml
index 1ef66e5b..6eea8844 100644
--- a/.github/workflows/build.yml
+++ b/.github/workflows/build.yml
@@ -40,3 +40,12 @@ jobs:
       with:
         name: gitbucket-java${{ matrix.java }}-${{ github.sha }}
         path: ./target/executable/gitbucket.*
+
+  selinux-check:
+    runs-on: ubuntu-latest
+    steps:
+    - uses: actions/checkout@v6
+    - name: Install SELinux tools
+      run: sudo apt-get update && sudo apt-get install -y checkpolicy
+    - name: Check SELinux policy syntax
+      run: checkmodule -M -m -o /dev/null contrib/linux/redhat/selinux/gitbucket.te
diff --git a/contrib/linux/redhat/selinux/gitbucket.te b/contrib/linux/redhat/selinux/gitbucket.te
index da3a5748..3125212b 100644
--- a/contrib/linux/redhat/selinux/gitbucket.te
+++ b/contrib/linux/redhat/selinux/gitbucket.te
@@ -1,12 +1,14 @@
-module gitbucket 1.0;
+module gitbucket 1.1;
 
 require {
 	type smtp_port_t;
 	type tomcat_t;
 	type tomcat_var_lib_t;
 	type unreserved_port_t;
+	type var_lib_t;
 
-	class file { execute };
+	class file { execute read write create delete getattr setattr lock append };
+	class dir { read write create delete getattr setattr lock add_name remove_name search };
 	class tcp_socket { name_bind };
 	class tcp_socket { name_connect };
 }
@@ -19,3 +21,7 @@ allow tomcat_t tomcat_var_lib_t:file { execute };
 
 # allow tomcat to serve repositories via SSH
 allow tomcat_t unreserved_port_t:tcp_socket { name_bind };
+
+# allow access to /var/lib/gitbucket
+allow tomcat_t var_lib_t:dir { read write create delete getattr setattr lock add_name remove_name search };
+allow tomcat_t var_lib_t:file { execute read write create delete getattr setattr lock append };
